# 📥 Downloading existing `datasets`

Use the `python -m modalbed.scripts.download --dataset="msr_vtt"` to automatically download the datasets.

# 🤝 Contributing more `datasets`

If you want to add more datasets, add download function in `modalbed/download.py`, and add the dataset class for loading in `modalbed/datasets.py`.

Welcome your pull request 😊!